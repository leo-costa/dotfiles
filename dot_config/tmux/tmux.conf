# Change default prefix
unbind C-b
set -g prefix C-s
bind-key C-s send-prefix

# Souce changes easier
unbind r
bind r source-file ~/.config/tmux/tmux.conf

# Allow mouse use
set -g mouse on

# Ensure neovim works well when pressing Ctrl + [
set -g escape-time 0

# Split panes use current working dir
bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"

# Move status bar up top
set -g status-position top

# Set window and pane indexes starting a 1
set -g base-index 1
setw -g pane-base-index 1
set-option -g renumber-windows on

# VI style selection mode and move around panes
set-window-option -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'catppuccin/tmux'

set -g @catppuccin_flavor 'mocha' # latte or frappe, macchiato, mocha
set -g @catppuccin_window_left_separator ""
set -g @catppuccin_window_right_separator " "
set -g @catppuccin_window_middle_separator " █"
set -g @catppuccin_window_number_position "right"
set -g @catppuccin_window_default_fill "number"
set -g @catppuccin_window_default_text "#W"
set -g @catppuccin_window_current_fill "number"
set -g @catppuccin_window_current_text "#W"
set -g @catppuccin_status_modules_right "directory session"
set -g @catppuccin_status_left_separator  " "
set -g @catppuccin_status_right_separator ""
set -g @catppuccin_status_fill "icon"
set -g @catppuccin_status_connect_separator "no"
set -g @catppuccin_directory_text "#{pane_current_path}"

# First run `git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm`
# Initialize TMUX plugin manager
run '~/.tmux/plugins/tpm/tpm'
